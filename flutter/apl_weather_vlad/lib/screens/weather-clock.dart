import 'package:flutter/material.dart';

class WeatherByTheClock extends StatelessWidget {
  const WeatherByTheClock({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(),
    );
  }
}
